function Scene(gui, driver) {
    this.gui = gui;
    this.driver = driver;

    // Don't await!
    this.update = function(deltaTime) {

    };

    // Don't await!
    this.render = function(deltaTime) {

    };
}